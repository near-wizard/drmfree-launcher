# 0007 — Project name

**Status:** decided — DRM-Free Launcher

The original brief carried this as "Codename: TBD." Locking in
**DRM-Free Launcher** (repo/package: `drmfree-launcher`) as the real
name — it's descriptive, on-thesis, and already used throughout the
codebase (window title, `package.json`, `Cargo.toml`), so there's no
rename cost to defer.
